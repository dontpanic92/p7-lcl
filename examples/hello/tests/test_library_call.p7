import test.test;
import lcl;

@test(expected_type = "string", expected_value = "Hello, example test from p7-lcl")
fn test_library_call() -> string {
    lcl.hello("example test")
}

@test(expected_type = "int", expected_value = "1")
fn test_library_version() -> int {
    lcl.binding_version()
}
